# v0.7 Lyrics Quality Baseline

日期：2026-08-31

## 数据口径

- 最新完整自然运行：run `50`，2026-08-31 00:00:17-00:19:41（Asia/Shanghai）。
- 数据集：`extended_1000`，`1000` 首、`6000` 条 Provider 结果。
- 热门代理集：`top_songs_weighted.tsv`，`300/300` 精确连接；它是 chart-derived proxy，不是真实用户 telemetry。

| 窗口 | Overall | Top Songs | Top 100 |
| --- | ---: | ---: | ---: |
| run 50 | `890/1000` (`89.0%`) | `272/300` (`90.67%`) | `97/100` (`97.0%`) |
| runs 43-50（严格 7x24h，8 runs） | `7128/8000` (`89.10%`) | `2174/2400` (`90.58%`) | `770/800` (`96.25%`) |
| runs 44-50（最近 7 runs） | `6236/7000` (`89.09%`) | `1901/2100` (`90.52%`) | `674/700` (`96.29%`) |

## Run 50 Provider

| Provider | Success | Unique contribution |
| --- | ---: | ---: |
| LRCLIB | 622 | 125 |
| LRCMux | 602 | 70 |
| NetEase | 521 | 38 |
| Kugou | 206 | 9 |
| QQ | 65 | 3 |
| Soda | 0 | 0 |

## Failure Taxonomy

Run 50 原始 provider-row 标签：`matching_failed 2520`、`invalid_response 1002`、
`api_unavailable 266`、`no_lyrics_found 194`、`unknown_error 2`。

分析层非互斥分类：`no-result 1591`、`parser 1002`、`network 19`、
`matcher rejection 971`、`ambiguity 401`。run 50 的 `110` 首 song-level miss 中，
Provider 证据出现数为：no-result `108`、parser `110`、matcher rejection `103`、
ambiguity `31`、network `5`。Soda 每首都 parser 失败，因此这些出现数不能当作互斥根因。

`wrong candidate` 当前不可计算：人工真值为 `0`，数据库也不保存 Production
最终 selected-candidate decision。confirmed false positive 也是未测量，不是零。

## Gate

- 不依据一次 run 调整 Provider priority。
- 不把 Coverage 当作 lyric correctness 或 Timing Quality。
- Production Matcher 继续冻结，直到 labeled holdout 证明 correct selection 提升且 confirmed false positive 为 `0`。
