# NotchMuse v0.6.1 Release Finalization Report

日期：2026-08-30

结论：GO

## Release Identity

- Release commit：`9180b78`
- Stability fix commit：`e3c2e53`
- Tag：`v0.6.1`
- Version / Build：`0.6.1` / `7`
- GitHub Release：<https://github.com/Xiye88/NotchMuse/releases/tag/v0.6.1>
- DMG SHA-256：
  `c36d51416759108abbc9fa41b20a48510b1208b7f9d0f72f8b240753bb8f52ed`

## Final Stability Gate

### Network Recovery

- Apple Music：PASS。断网时切歌未 crash；恢复网络并再次切歌后歌词恢复，
  无持续 stale lyrics。
- Spotify：PASS。使用确认运行中的 build 7 候选 App 重测；断网与恢复后的
  切歌均正常，App 全程存活，无持续 stale lyrics。

### Sleep / Wake

- Apple Music + Status Bar Mode：PASS。唤醒、恢复播放和下一首均显示当前歌词。
- Spotify + Notch Mode：PASS。唤醒、恢复播放和下一首均显示当前歌词，
  无 Notch 假死或旧歌词残留。

## Build Verification

- Clean Release build：PASS
- English self-test：PASS
- `zh-Hans` self-test：PASS
- Release Matcher diagnostics scan：PASS，未发现 DEBUG decision events
- Architecture：`arm64`
- Code signing：ad-hoc signature verified
- `hdiutil verify`：PASS

## Public Artifact Verification

从 GitHub Release 重新下载 `NotchMuse.dmg` 和 checksum 后完成：

1. `shasum -a 256 -c`：PASS
2. `hdiutil verify`：PASS
3. Mount：PASS
4. Version / Build：`0.6.1` / `7`
5. Mounted binary 与本地最终 Release binary：byte-identical
6. 安装到 `/Applications`：PASS
7. Apple Music + Status Bar live lyrics：PASS
8. Spotify + Notch Mode live lyrics：PASS

## Scope Boundary

- Production Matcher 未修改。
- Provider order、retry、threshold 和 normalization 未修改。
- 未新增音乐平台或 Lyrics Provider。

## Known Minor Risk

- Spotify desktop client 自身在网络切换后可能显示 `Something went wrong`，
  需要正常点击 `Reload page`；客户端恢复后 NotchMuse 自动恢复歌词。该行为未造成
  NotchMuse crash 或持续 stale state，不阻塞 v0.6.1。

## Final Decision

`v0.6.1` 已发布并通过远端 artifact 验证。v0.6 Apple Music Integration、
v0.6.0 Post-Release Stability Validation 与 v0.6.1 Stability Patch 可统一归档。
