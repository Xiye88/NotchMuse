# Candidate Ambiguity Report

日期：2026-08-31

## 样本

从 run 32 `ambiguous_gap` 中审计 `40` 例：20 个 tie、20 个 gap `1-5`；
Kugou `15`、NetEase `15`、LRCLIB `8`、QQ `2`。

| 分类 | 数量 | 判断 |
| --- | ---: | --- |
| A 同曲重复 candidate | 1 | `32:833:QQ` 的 title、artist、duration、派生 ID 均相同；仅确认冻结证据级重复 |
| B 不同版本但歌词相同 | 0 | 无 lyric text/timing，不能判真 |
| C 不同版本且歌词不同 | 0 | 无 lyric text/timing，不能判真 |
| D Metadata 不足 | 39 | 缺 candidate album/ISRC/native ID/歌词正文/第三候选 |

版本冲突代表样本：`32:17:Kugou`、`32:17:NetEase`、`32:18:NetEase`、
`32:121:NetEase`、`32:283:NetEase`、`32:285:LRCLIB`、`32:59:LRCLIB`、
`32:320:LRCLIB`、`32:518:QQ`。

974 个完整 pair 中，当前 parser 判定 identity-equivalent `350`；其中
`ambiguous_gap` 为 `324/397`，但 title+artist+duration+派生 ID 全同只有 `1`。
run 19 的 `464/489` 属于另一历史批次，不能并入 run 32 分母。

## 决策

Candidate deduplication simulation：`NO-GO`。当前证据不足以证明 350 个
metadata-equivalent pair 的歌词和版本相同。先做 strategy-blind 人工标签与歌词
正文对比，不能直接把等价 metadata 当作重复候选。
