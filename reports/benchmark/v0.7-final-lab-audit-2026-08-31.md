# v0.7 Final LAB Audit

日期：2026-08-31

## Top Songs Miss Root Cause

Run 50 有 `28/300` 首 Top Songs miss，其中 Top 100 为 `3/100`。

| Primary owner | Top Songs | Top 100 |
| --- | ---: | ---: |
| Provider | `1/28` (`3.57%`) | `1/3` (`33.33%`) |
| Matcher | `0/28` (`0%`) | `0/3` (`0%`) |
| Metadata limitation | `27/28` (`96.43%`) | `2/3` (`66.67%`) |
| Network / Parser | `0/28` (`0%`) | `0/3` (`0%`) |

Primary owner 是严格互斥结论。只有 `Nakorn Dara` 在所有 Provider 中都没有
candidate evidence，因此可确认 Provider 主责。其余 27 首虽出现候选、reject 或
ambiguity，但缺 candidate album、ISRC、provider stable ID 和歌词正文，无法确认
候选是否正确，因此归 Metadata limitation，不能算作 Matcher bug。

非互斥共现证据：parser `28/28`、no-result `27/28`、matcher rejection `26/28`、
provider unavailable `15/28`、ambiguity `6/28`、network transient `0/28`。
parser `28/28` 来自 Soda 的系统性空响应，不能解释其他 Provider 是否本可命中。

## Top 3

1. Metadata insufficient：27 首主责。
2. Provider no result：1 首确认主责，27 首有共现证据。
3. Ambiguity / rejection：分别 6/28、26/28 共现，但 `0` 首能确认 Matcher 拒绝了正确 candidate。

## Soda Provider

结论：`REMOVE-CANDIDATE`，仅为后续生产决策建议，本轮不改顺序或代码。

- run 50：success `0/1000`、unique contribution `0`、invalid response `1000/1000`。
- runs 43-50：success `0/8000`、unique `0`、parser failure `7996/8000`。
- 2026-08-31 只读探测返回 HTTP `200`、`Content-Type: application/json`、
  `Content-Length: 0`；当前 JSON parser 对空 body 抛错符合观察。
- endpoint 使用固定 device/install ID 和非公开 Luna PC 参数，不具备稳定 API 契约。

数据集：`lyrics-provider-benchmark/data/v0.7_run50_top_songs_root_cause.csv`。
