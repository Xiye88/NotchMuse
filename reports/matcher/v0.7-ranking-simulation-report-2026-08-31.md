# Offline Ranking Simulator v2 Report

日期：2026-08-31

## 可计算边界

冻结集 `2568` 条全部未标注。官方 evaluator 结果为 `eligible=0`、
`missing_ground_truth=2568`。correct candidate rate、wrong candidate rate、
false positive rate 均为 **NOT COMPUTED**。

当前 `_old_selection()` 只识别 `top/second`，而冻结行是
`no_candidates=1211`、`below_threshold=960`、`ambiguous_gap=397`；SQLite loader
也只读取 `matching_failed`，不包含 Current Matcher 已成功选择的 case。因此它
只能模拟“失败池恢复”，不能完整比较 Current 与 Experimental ranking。

## 结构性选择代理

下表只表示 signal 会让多少完整 pair 选择 top/second，不表示选得正确。

| Signals | Top | Second | Unresolved |
| --- | ---: | ---: | ---: |
| Duration | 599 | 267 | 108 |
| Artist exact | 61 | 26 | 887 |
| Live guard | 80 | 11 | 883 |
| 当前已实现 title signals | 94 | 26 | 854 |
| Duration + artist | 609 | 265 | 100 |
| 全部当前已实现 signals | 623 | 272 | 79 |

Album experiment 有 `1948` 次 side-level unavailable。Remix、Acoustic、OST、
Cover、with、translation、anniversary、radio edit 尚未实现；当前 Remaster/feat
奖励逻辑也不具备安全证明。

## Gate

- Strategy-blind labeling 与 evaluator 口径修正：`CONDITIONAL GO`。
- Experimental ranking：`NO-GO`。
- Production Matcher：`NO-GO`。
- confirmed false positive 当前是“不可计算”，不是 `0`。

下一步只允许：补齐 labeled recovery pool 与独立 holdout；修正
`ambiguous_unresolved` 命名/统计；让 Current baseline 包含真实已选择 case。
在这些工作完成前不允许修改 score、threshold、normalization 或 dedup 行为。
