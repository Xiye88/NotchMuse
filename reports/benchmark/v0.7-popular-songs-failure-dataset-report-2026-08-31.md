# Popular Songs Failure Dataset

日期：2026-08-31

## Freeze

- 文件：`lyrics-provider-benchmark/data/v0.7_popular_songs_failure_dataset.tsv`
- `50` 行，`50` 个唯一 track ID，无重复补数。
- 选择规则：先取 run 50 的 28 个 Top Songs miss，再按自然 run 倒序加入尚未出现的 miss，至 run 20 首次达到 50 条。
- discovery run：`50:28`、`49:1`、`48:2`、`47:1`、`34:1`、`25:1`、`20:16`。
- 分类：Chinese pop `20`、Korean `15`、spotify_hot chart proxy `10`、Japanese `3`、English pop `2`。

## Field Coverage

- title/artist/album/duration/category/reject/failure map：`50/50`。
- candidate-backed Provider evidence 与 top score：`49/50`。
- second score：`46/50`。
- player_source：`0/50`，Benchmark 未采集；`spotify_hot` 只是 chart category。
- selected_candidate：`0/50`，这些 case 没有被覆盖，DB 也不保存 Production 最终选择。
- candidate album、ISRC、第三及后续 candidate：schema 不存在。
- human wrong-candidate label：`0/50`。

`failure_classification` 是 provider-row 证据的非互斥集合，不是 song-level 单一根因。
本数据集适合下一步 strategy-blind 人工标签与 Provider/Matcher 边界复核，不足以直接训练或批准 Production ranking。
